const initState = {
  users: [
    { id: 1, name: 'Red' },
    { id: 2, name: 'Greg' },
    { id: 3, name: 'James' },
  ],
}

const authReducer = (state = initState, action) => {
  return state
}

export default authReducer
